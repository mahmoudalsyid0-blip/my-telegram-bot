# Telegram bot framework (async/await, v20+)
# v22.4+ contains the official fix for Python 3.14's stricter asyncio.get_event_loop()
# behavior (PTB issue #4874). v21.x does NOT have this fix — do not downgrade.
python-telegram-bot==22.8

# Media downloader
yt-dlp==2024.11.18

# Async HTTP (used internally by python-telegram-bot)
httpx==0.27.2
anyio==4.6.2
